/**
 * Public branding constants used in outbound email.
 *
 * Kept next to the notification layer because the API cannot import web
 * constants. Update the URL here when the account gets a short username.
 */
export const PLATFORM_FACEBOOK_URL =
  'https://www.facebook.com/profile.php?id=61593503415341';

/** Label of the social link rendered in the email footer. */
export const PLATFORM_FACEBOOK_LABEL = 'Facebook';
